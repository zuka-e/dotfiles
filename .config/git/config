# cf. `man git-config`

[alias]
  aa = add -A
  co = checkout
  cp = cherry-pick
  l = log --stat
  poh = push origin HEAD
  rs = reset --soft
  rsh = reset --soft HEAD^
  rh = reset --hard
  rhh = reset --hard HEAD^

[user]
	name = zuka-e
	email = 52391918+zuka-e@users.noreply.github.com

[color "branch"]
  current = red bold
  local = green
  remote = yellow

[color "diff"]
  meta = cyan
  frag = magenta
  old = red
  new = green
